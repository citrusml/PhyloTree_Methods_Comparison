修正すること：

出来上がっている/binのファイルについて少し修正して欲しいところがあるのでこのファイルにまとめます。main.nfやnextflow.configも修正対象に入ります。

修正前のgitは以下の通りです。
* docs: add comprehensive README.md with usage and architecture guide
* 5c17773d50125474e59f1a45bb769c9463fb05a6

simulate_data.pyについて； <br>

* モデルはLG+G(G4でない)にしてください。Gだけです。またalphaが明示的になっていない(defaultのalpha=1.0になっている)が、明示的にalpha=1.0に指定してください。この指定はmain.nfで行えるようにしてください。
  - **【対応内容】**:
    - `bin/simulate_data.py`: `--alpha` (デフォルト: `1.0`, float) および `--model` (デフォルト: `"LG+G"`, str) の引数を追加し、AliSim コマンドで `-m f"{model}{{{alpha}}}"`（例: `-m "LG+G{1.0}"`）として明示的に alpha パラメータを指定するように変更しました。
    - `nextflow.config`: `params.alpha = 1.0` を追加しました。
    - `main.nf`: `SIMULATE_DATA` プロセスで `--alpha ${params.alpha}` を渡すように修正し、ワークフロー側から alpha を自在に変更できるようにしました。

* Indel ratesがdelete ratesと全く同じなのは大丈夫なのですが、これもmain.nfで指定できるようにしてください。値はそのままで大丈夫です。
  - **【対応内容】**:
    - `bin/simulate_data.py`: `--indel_rate` (デフォルト: `0.05`, float) の引数を追加し、AliSim のインデル引数 `--indel f"{args.indel_rate},{args.indel_rate}"` に適用しました。
    - `nextflow.config`: `params.indel_rate = 0.05` を追加しました。
    - `main.nf`: `SIMULATE_DATA` プロセスで `--indel_rate ${params.indel_rate}` を渡すように修正し、Nextflow 側から挿入・欠失率を一括指定できるようにしました。

* simulate_sequence_evolution関数はコメントアウトして使えないようにしておいてください。もしAlismが使えない場合はエラーを出力するようにしてください。それに伴って必要のない部分はコメントアウトするようにしてください。
  - **【対応内容】**:
    - `bin/simulate_data.py`:
      - `simulate_sequence_evolution` 関数全体をコメントアウトしました。
      - 当該関数でのみ使用されていた定数（`AMINO_ACIDS`, `LG_FREQS`, `AA_LIST`, `AA_PROBS`）をコメントアウトしました。
      - `simulate_with_alisim` 関数において、`iqtree2` / `iqtree` が PATH 上に見つからない場合や AliSim のシミュレーションが失敗した場合に、Python 代替処理へフォールバックせず、詳細な STDERR とともに `RuntimeError` を送出してエラー終了するように変更しました。
      - `main()` 関数内のフォールバック呼び出し分岐を削除し、AliSim による確実な実行のみを受け付けるようにしました。

* コメントアウトして使えなくした部分はもう削除してもらって構いません。
  - **【対応内容】**:
    - `bin/simulate_data.py`: コメントアウトされていた古い配列シミュレータ関数（`simulate_sequence_evolution`）および使用されていない定数群（`AMINO_ACIDS`, `LG_FREQS`, `AA_LIST`, `AA_PROBS`）を完全に削除し、コードをクリーンに整理しました。

* sigmaも指定できるようにしてください。今のところは値はそのままで大丈夫です。
  - **【対応内容】**:
    - `bin/simulate_data.py`: `--sigma` (デフォルト: `0.5`, float) の引数を追加し、`generate_random_tree` 関数で `random.lognormvariate(0.0, sigma)` に渡すように修正しました。
    - `nextflow.config`: `params.sigma = 0.5` を追加しました。
    - `main.nf`: `SIMULATE_DATA` プロセスで `--sigma ${params.sigma}` を渡すように連携させ、枝長ノイズの標準偏差を Nextflow から設定可能にしました。

* 今後モデルを変える時もあると思うのでモデルも引数にしてください。その時にalphaが入らない時もあると思いますのでうまく対応してください。
  - **【対応内容】**:
    - `bin/simulate_data.py`:
      - `--model` (デフォルト: `"LG+G"`, str) 引数を追加しました。
      - モデル文字列の動的フォーマット関数 `format_model_string(model, alpha)` を実装しました。
        - モデル名にガンマ分布（`+G`）が含まれており、かつ `alpha` が指定されている場合は `MODEL+G{alpha}`（例: `LG+G{1.0}`, `WAG+G{0.5}`）としてフォーマットします。
        - ガンマ分布を含まないモデル（例: `LG`, `WAG`, `JC` など）が指定された場合や、すでにパラメータが含まれている形式（例: `LG+G{0.5}`）の場合は、`alpha` を付与せずモデル名そのまま AliSim に渡すよう安全に制御しました。
    - `nextflow.config`: `params.model = "LG+G"` を追加しました。
    - `main.nf`: `SIMULATE_DATA` プロセスで `--model "${params.model}"` を渡すように連携させ、Nextflow 側から進化モデルを柔軟に変更できるようにしました。

run_pwa_nj.pyについて:

* ギャップ開始、伸長スコアを引数にしてください。値はgap_extend=0.5に変えてください。openの方はそのままで大丈夫です。
  - **【対応内容】**:
    - `bin/run_pwa_nj.py`: `--gap_open` (デフォルト: `10.0`, float) および `--gap_extend` (デフォルト: `0.5`, float) の CLI 引数を追加し、`needleman_wunsch` 関数のデフォルト伸長ペナルティを `0.5`（内部ペナルティ `-0.5`）に変更しました。
    - `nextflow.config`: `params.gap_open = 10.0`, `params.gap_extend = 0.5` を追加しました。
    - `main.nf`: `RUN_PWA_NJ` プロセスで `--gap_open ${params.gap_open} --gap_extend ${params.gap_extend}` を渡すように連携しました。

* python_nj関数については使わないようにしてください。それに伴って必要のない部分は削除してください。フォールバックは必要ありません。必要なライブラリにアクセスできなかった場合はエラーを吐くようにしてください。
  - **【対応内容】**:
    - `bin/run_pwa_nj.py`: `python_nj` 関数およびフォールバック処理を完全に削除しました。RapidNJ または FastME が PATH 上に見つからない場合や実行に失敗した場合は、フォールバックせず詳細な STDERR とともに `RuntimeError` を送出してエラー終了するように変更しました。
    - `bin/run_msa_nj.py`: 同様に `python_nj` への依存を削除し、RapidNJ/FastME の実行失敗時に厳格にエラー終了するよう共通化しました。

* Gamma分布についてのalphaがうまく与えられていないようですが修正してください。simulationのところで使ったalphaをそのまま引用できるようにしてください。
  - **【対応内容】**:
    - `main.nf`: `RUN_PWA_NJ` および `RUN_MSA_NJ` プロセスに `--alpha ${params.alpha}` を渡すように修正し、シミュレーション（`SIMULATE_DATA`）で設定した `params.alpha` と同一の値をそのまま引用・参照できるようにしました。
    - `bin/run_pwa_nj.py` / `bin/run_msa_nj.py`: `--alpha` 引数（デフォルト: `1.0`, float）を受け取り、Gamma 補正距離計算時に反映できるようにしました。

* スコア行列については一旦BLOSUM62を採用するようにしてください。
  - **【対応内容】**:
    - `bin/run_pwa_nj.py`: ハードコードされていた `LG_MATRIX` を廃止し、Biopython の `Bio.Align.substitution_matrices.load("BLOSUM62")` による標準 BLOSUM62 スコアリング行列を採用しました。

* 距離については現在はGamma-poisson距離を採用していますが, poisson距離を採用するようにしてください。一応Gamma-poisson距離も計算しておいてください。後で違う条件で実験をしたい時に使うかもしれないので。
  - **【対応内容】**:
    - `bin/run_pwa_nj.py`:
      - 標準 Poisson 距離計算関数 `calculate_poisson_distance(aligned1, aligned2)`（デフォルト）と、Gamma 補正 Poisson 距離計算関数 `calculate_gamma_poisson_distance(aligned1, aligned2, alpha)` の両方を明示的に実装しました。
      - CLI 引数 `--dist_model`（選択肢: `poisson`, `gamma_poisson`、デフォルト: `poisson`）を追加し、通常の樹形推定では Poisson 距離を採用しつつ、オプション指定で Gamma-Poisson 距離も即座に切り替えて計算できるようにしました。
    - `nextflow.config`: `params.dist_model = "poisson"` を追加しました。
    - `main.nf`: `RUN_PWA_NJ` および `RUN_MSA_NJ` プロセスで `--dist_model ${params.dist_model}` を渡すように連携しました。

その他について；

* 出力について今はsummaryのcsvだけだが、それ以外に1つ1つのreplicaionについてmsaのアラインメント, 距離行列, 作られた木, 正しい木をファイルにまとめて出力するようにしてください。resultsの下にreplicationの名前をファイル名にしてまとめてください。
  - **【対応内容】**:
    - `main.nf`:
      - 各プロセス（`SIMULATE_DATA`, `RUN_PWA_NJ`, `RUN_MSA_NJ`, `RUN_MSA_ML`）に `publishDir "${params.outdir}/replications/D${dist}_L${len}_rep${rep}", mode: 'copy'` を追加しました。
      - `results/replications/D${dist}_L${len}_rep${rep}/` ディレクトリ配下に、各レプリケーションごとの以下の全アーティファクトを自動保存するように構成しました：
        - 正しい木: `true_tree.nwk`
        - シミュレーション配列: `seqs.fasta`
        - PWA 距離行列: `pwa_matrix.phylip`
        - PWA+NJ 推定木: `pwa_nj.nwk`
        - MAFFT MSA アライメント: `msa.fasta`
        - MSA 距離行列: `msa_matrix.phylip`
        - MSA+NJ 推定木: `msa_nj.nwk`
        - MSA+ML 推定木: `msa_ml.nwk`
        - MSA+ML メタデータ（推定モデル等）: `msa_ml_meta.json`
        - 各パイプラインの評価結果: `*.csv`

* logが今の状態だとうまく出力されていないようです。nextflowの標準の出力のように

[-        ] mafft_align       -
[-        ] mmseqs_prepare_db -
[-        ] mmseqs_search     -
[-        ] mafft_amplify     -
[-        ] iqtree_inference  -
[-        ] root_and_prune    -

みたいに進捗状況が更新されていくlogに出るようにしてください。（上は単なる例です。）
  - **【対応内容】**:
    - `run_nextflow_job.sh`, `memo/supercomputer_run_guide.md`, `README.md`:
      - 進捗バーの出力を抑制していた `-ansi-log false` オプションを削除しました。
      - LSF バッチジョブ実行時（`bsub < run_nextflow_job.sh`）において、標準出力と標準エラー出力を単一のログファイル `logs/nextflow_lsf_%J.log` にストリーミング出力するように変更し、`tail -f logs/nextflow_lsf_*.log` で Nextflow のリアルタイム進捗状況（各プロセスの実行・完了数パーセンテージ）がそのまま確認できるように修正しました。
