# 20260812-19


```sh
git clone https://github.com/citrusml/PhyloTree_Methods_Comparison.git
curl -Ls https://micro.mamba.pm/api/micromamba/linux-64/latest | tar -xvj

citrusml2004@mafftweb:~$ export PATH="$HOME/bin:$PATH"
citrusml2004@mafftweb:~$ micromamba --version
2.9.0

micromamba create -f environment.yml

# ローカルでnwの編集を行ったので
git pull
nextflow run main.nf -profile local -resume

nextflow run main.nf -profile supercomputer -bg > logs/run_newxtfllw.log
# なぜか一部のファイルしかなかった

# こっちだとしっかりlogが保存される。
nohup nextflow run main.nf -profile supercomputer -resume > logs/run_nextflow.log 2>&1 &

# これで正しく動くかも->logはもともと正常だった。
nohup nextflow run main.nf -profile supercomputer -resume -ansi-log false > logs/run_nextflow.log 2>&1 < /dev/null &

# nextflow直打ちがあまり良くなかったらしくrun_nextflow_job.shを作成してそれを実行するようにする。
bsub < run_nextflow_job.sh
```


とりあえず動くようになたので一度ファイルをしっかり見直して意図通りに設計されていないところを見つける。


なんか少しだけスパコン側の異常が出ていそうなので一旦bkill 0してして再開してみる。
ややこしくなるかもだけど一旦run.shをスパコンで編集します。
```sh
bkill 0
bsub < run_nextflow_job.sh
```

終わりはしたけど、ここからが本番。ansi-log falseがあったから今まで見たいなログが出なかったようだ。
nextflow周りのファイルの変更も行ったのでここから回してみる。途中でkillされる計算がたまにあるようだからresumeは必須かも。

進化距離が深く、配列長が短い時に全欠失が生じていた。それを直して再実行した。普通なら2,3hで終わりそう。
配列長の条件を変えてみました。

なんかクラッシュがところどころクラッシュが起きてしまっているようであった。これが先生が言っていたやつなのだろうか？もう少し詳しく調べてみる。

やっとできた。何回かやり直しが必要だった。

```sh
# 1. 成果物を results/replications/ にハードリンク整理（一瞬で完了）
python bin/organize_replications.py --mode hardlink

# 2. 整理されたファイルを確認
ls -l results/replications/D0.1_L100_rep1/

# 3. 不要になった巨大な work フォルダを完全に削除（容量解放）
rm -rf work/ .nextflow/
```

```sh
cd ~/PhyloTree_Methods_Comparison
git pull

# -------------------------------------------------------------
# 1. 残基数（配列長）の解析スクリプト
# -------------------------------------------------------------
# results/replications/ を指定する場合：
python bin/analyze_sequence_lengths.py --data results/results_ver2/replications --outdir results/results_ver2/length_analysis

# -------------------------------------------------------------
# 2. ボックスプロット（平均・分散ラベル付き）の作成
# -------------------------------------------------------------
python bin/plot_nrf_boxplots.py --csv results/results_ver2/benchmark_summary.csv --outdir results/results_ver2/figures
```
ver2はIndel rateが0.05

流石にindelとdeletionが大きすぎるので0.01に変更してもう一度やり直してみる。新しいファイルは作らずに既存のファイルを書き換えることで対応した。
やり直しを実行明日の朝までにできていると良いなという感じ.

```sh
cd ~/PhyloTree_Methods_Comparison
git pull

# -------------------------------------------------------------
# 1. 残基数（配列長）の解析スクリプト
# -------------------------------------------------------------
# results/replications/ を指定する場合：
python bin/analyze_sequence_lengths.py --data results/results_ver3/replications --outdir results/results_ver3/length_analysis

# -------------------------------------------------------------
# 2. ボックスプロット（平均・分散ラベル付き）の作成
# -------------------------------------------------------------
python bin/plot_nrf_boxplots.py --csv results/results_ver3/benchmark_summary.csv --outdir results/results_ver3/figures
```

ver3はrateが0.01

これでも普通に長さ1になっているやつがあったけど一旦続けるか...

* Taxon 数 $N$（4 通り）: [8, 32, 64, 128]
* 進化距離 $D$（5 通り）: [0.1, 0.5, 1.0, 2.0, 3.0]
* 配列長 $L$（3 通り）: [100, 500, 1000]

```sh
bsub < run_scripts/run_nextflow_taxon.sh 
```

全然終わらない